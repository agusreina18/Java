package sincronica;

public class Mesa {

    public static void main(String[] args) {
	int numero;
    System.out.println();
    }

    public static boolean esDivisible(int n, int
            divisor){
        if ()
    }

    public static boolean esPrimo(int num){
        if (num.)
    }
}

/*static boolean esDivisible(int n, int divisor){
            return (n % divisor) == 0;
        }

        static boolean esPrimo (int numero){
            boolean resultado = false;
            for(Integer i = 2; i <= Math.sqrt(numero);i++){
                resultado = resultado || esDivisible(numero, i);
            }
            return !resultado;
        }
*/

//ejercicio 2//
/*static Integer maximoEntreTresNumeros(Integer unNumeroA, Integer unNumeroB, Integer unNumeroC){
            Integer resultado;
            if (unNumeroA.compareTo(unNumeroB) > 0){
                resultado = unNumeroA;
            } else { resultado = unNumeroB;}
            if (resultado.compareTo(unNumeroC) < 0){
                resultado = unNumeroC;
            }
            return  resultado;
        }
*/
